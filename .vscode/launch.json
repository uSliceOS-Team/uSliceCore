{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug example (gdb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/examples/logic_controlled_tasks/example",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "build: example (host g++)",
            "stopAtEntry": false
        }
    ]
}
